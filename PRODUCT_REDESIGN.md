# Pulse 产品重构判断

## TL;DR

Pulse 不应要求用户先维护项目系统，再从系统生成周报。用户唯一需要提供的是日常工作中已经存在的文档链接；系统负责读取、归类、提炼和持续更新项目事实，最终自然生成 Todo 与周报。产品首页因此只保留一个主输入和一组紧凑的项目进展，其他能力退到结果和上下文中。

## 当前方案为什么不好用

当前产品把项目、资料、Agent、同步和汇报设计成平级模块。功能虽然完整，但用户必须先判断“我应该进入哪个模块”，再理解字段和系统结构，最后才能完成一次同步。问题不在视觉复杂，而在系统以数据模型为中心，而不是以用户任务为中心。

## 新的核心机制

主路径收敛为：粘贴链接 → Agent 读取正文 → 识别或创建项目 → 提炼核心进展 → 提取下一步 Todo 与负责人 → 累积为周报素材。用户只确认 Agent 无法确定的信息，不再重复录入文档中已有的内容。

首页只回答三个问题：现在发生了什么、接下来谁做什么、哪些项目需要关注。项目颜色用于建立快速识别，不代表健康度；健康度以克制的文字状态表达，避免颜色同时承担多个语义。

## 数据与账号如何分开

用户是稳定身份，工作空间是数据边界，项目属于工作空间，成员通过工作空间关系获得访问权限。一个用户可以创建或加入多个工作空间，同一个项目可以被空间内成员协作，但不能被其他空间查询。所有写入继续记录真实操作者，Bot 与 Agent 作为来源而不是数据所有者。

## MVP 验证

本阶段只验证一件事：用户是否愿意持续把链接交给 Pulse，并直接采用系统生成的项目进展与 Todo。核心指标应关注链接提交成功率、自动归类后无需修改的比例、Todo 负责人确认率和周报生成后的复制使用率，而不是页面访问量或功能点击量。
